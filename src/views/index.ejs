<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Test</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <h1>elllooo</h1>
    <ul>
      <% Character.forEach(Character => { %>
      <li>
        <h2><%= character.name %></h2>
        <%user.userInstruments.forEach(userInstruments => { %>
        <p><%= userInstruments %></p>
        <% }); %>
      </li>
      <% }); %>
    </ul>
  </body>
</html>
